{port_specs, [{"priv/granderl_nif.so", ["c_src/granderl.c"]}]}.

{port_env, [{"LDFLAGS", "$LDFLAGS"},
            {"CFLAGS", "$CFLAGS -Wall -Wextra -pedantic -O3 -march=native -ggdb"}]}.

{plugins, [pc]}.

{provider_hooks,
 [{pre,
   [{compile, {pc, compile}},
    {clean, {pc, clean}}]
  }]}.
